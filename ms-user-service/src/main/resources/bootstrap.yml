spring:
  application:
    name: ms-user-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  profiles:
    active: development

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
